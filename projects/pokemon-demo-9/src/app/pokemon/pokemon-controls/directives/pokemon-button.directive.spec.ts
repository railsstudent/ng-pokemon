import { PokemonButtonDirective } from './pokemon-button.directive';

describe('PokemonButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonButtonDirective();
    expect(directive).toBeTruthy();
  });
});
